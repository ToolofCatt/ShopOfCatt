# URL API cho trình duyệt gọi
NEXT_PUBLIC_API_URL=http://localhost:3001/api
# URL API cho server component gọi (nội bộ)
API_URL=http://localhost:3001/api
# Tên cửa hàng hiển thị trên web
NEXT_PUBLIC_SITE_NAME=Digital Store

# Tên miền công khai — dùng cho thẻ chia sẻ mạng xã hội, robots.txt và sitemap.xml.
# NHÚNG VÀO BUNDLE LÚC BUILD → đổi thì phải build lại.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
