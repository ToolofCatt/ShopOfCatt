# deps
node_modules/

# builds
dist/
dist-seed/
.next/
.turbo/
out/

# env
.env
.env.local
.env.*.local

# local database (embedded postgres)
apps/api/pgdata/

# Bản sao lưu dữ liệu — chứa key sản phẩm dạng rõ, email khách và hash mật khẩu.
# TUYỆT ĐỐI không đưa vào version control.
backups/
*.dump
*.sql.gz

# File tạm khi kiểm thử
*.tmp.js
*.tmp.mjs
*.tmp.ts

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# misc
.DS_Store

*.tsbuildinfo

# Repo dùng pnpm — package-lock.json chỉ xuất hiện khi ai đó lỡ gõ `npm install`,
# và hai lockfile song song là hai cây phụ thuộc khác nhau tuỳ người build.
package-lock.json
yarn.lock
